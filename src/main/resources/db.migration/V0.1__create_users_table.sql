CREATE TABLE yachts (
    id              SERIAL PRIMARY KEY,
    name            VARCHAR,
    family_name     VARCHAR,
    mobile_phone    VARCHAR,
    office_phone    VARCHAR,
    home_phone      VARCHAR,
    email           VARCHAR,
    password        VARCHAR
);
